apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-kafka.fullname" . }}
  labels:
    {{- include "test-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-kafka.selectorLabels" . | nindent 4 }}
